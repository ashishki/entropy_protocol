# ADR Directory

Architectural Decision Records for Trader Risk Audit are append-only.

Use an ADR when changing runtime tier, capability profile status, rule semantics, data retention policy, report claim boundaries, external integrations, or any rule in `docs/IMPLEMENTATION_CONTRACT.md`.
